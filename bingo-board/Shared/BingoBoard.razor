@using Model = bingo_board_model;
@if (Board != null)
    {
      
        <div id="title">@Board.Title</div>
        <div id="container">
            @foreach (var item in Board.BingoBoardItems)
                {
                    <BingoBoardItem Text="@item.Text"></BingoBoardItem>
                }
            
            
        </div>
    }

@code {
    [Parameter]
    public Model.BingoBoard Board {get;set;}
}